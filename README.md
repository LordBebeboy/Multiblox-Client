# Multiblox-Client
A next-generation fleet manager for Roblox. Run 35+ concurrent instances with automated crash recovery, session isolation, and 'Bus Driver' join orchestration. Built with .NET 10 &amp; WPF.
